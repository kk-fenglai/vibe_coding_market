# 模块地图（saas-starter）

把工程分成两层：**CORE 通用平台层（复用）** 与 **BUSINESS 业务示例层（DELF，可替换/可删）**。
本骨架由 `delf-B2-website` 抽取而来，已脱敏（无 `.env`、无真实密钥、无内部文档、无题库大文件）。

> **边界实现方式**：为避免移动文件破坏数十处相对 `require`，业务文件**仍按原目录平铺**在 `src/` 下，
> 模块边界用**注释围栏**（`↓↓↓ 业务示例 … ↑↑↑`）+ 本文件的**删除清单**标注。
> 想物理隔离时，可后续再按本表把 BUSINESS 文件挪进 `src/modules/business-delf/` 并修正引用。

---

## CORE 通用平台层（9 个模块）

| 模块 | 后端 | 前端 | Prisma 模型 | 依赖的 env |
|------|------|------|------------|-----------|
| **M1 用户登录系统** | `routes/auth.js` `routes/passwordReset.js` `middleware/auth.js` `middleware/requirePlan.js` `services/refreshTokens.js` `services/trial.js` `utils/{jwt,passwordPolicy,requestCountry}.js` | `pages/{Login,Register,ForgotPassword,ResetPassword,VerifyEmail,VerificationSent,ChangePassword}.tsx` `components/PasswordStrengthBar.tsx` `stores/{auth,geo}.ts` `api/{client,baseUrl}.ts` `utils/passwordPolicy.ts` | User, RefreshToken, EmailVerificationToken, PasswordResetToken, TwoFactorToken, LoginHistory | JWT_*, SMTP_*（验证/找回邮件） |
| **M2 支付** | `routes/payments/*` `routes/adminPayments.js` `services/payments/{stripe,wechat,alipay,reconcile}.js` `services/{billing,billingPolicy}.js` `constants/pricing.js` `constants/planMatrix.js`(PLAN_ORDER 部分) | `pages/{Pricing,Orders,StripeCheckout*}.tsx` `pages/admin/payments/*` `pages/admin/AdminPayments.tsx` `components/UpgradeDifferenceCard.tsx` | Product, Price, PriceStripeMapping, PaymentOrder, PayContract, RefundOrder, Subscription | STRIPE_*，可选 ENABLE_DIRECT_*/WECHAT_*/ALIPAY_* |
| **M3 后台管理** | `routes/{adminAuth,adminUsers,adminStats}.js` `middleware/admin.js` | `pages/admin/{AdminLogin,AdminDashboard,AdminUsers,AdminUserDetail,AdminLogs,AdminLoginHistory,AdminChangePassword}.tsx` `components/{AdminLayout,RequireAdmin}.tsx` `stores/adminAuth.ts` `api/adminClient.ts` | AdminLog, AppSetting | ADMIN_INITIAL_PASSWORD, ADMIN_EMAIL, ADMIN_IP_ALLOWLIST, JWT_ADMIN_* |
| **M4 日志** | `utils/logger.js`（pino + httpLogger + 字段脱敏 + requestId） | — | （审计 = AdminLog） | LOG_LEVEL |
| **M5 邮件** | `services/mailer.js` `scripts/testSmtp.js` | — | — | SMTP_* |
| **M6 反馈/意见箱** | `routes/feedback.js` `routes/adminFeedback.js` | `components/FeedbackWidget.tsx` `pages/admin/AdminFeedback.tsx` | Feedback | — |
| **M7 安全/基础设施** | `config/env.js` `middleware/errorHandler.js` `prisma.js` `src/index.js`(装配/helmet/cors/限流/health/优雅关停) | — | — | NODE_ENV, PORT, FRONTEND_URL |
| **M8 前端外壳** | — | `App.tsx` `main.tsx` `components/{AppLayout,PageLoader,LanguageSwitcher}.tsx` `i18n/*` `styles/*` `types/index.ts` + vite/tailwind/postcss/tsconfig | — | VITE_API_ORIGIN |
| **M9 部署** | `Dockerfile` `fly.toml` `env.example` | `vercel.json` | — | （部署平台变量） |

> 注：`middleware/requirePlan.js`、`constants/planMatrix.js`、`services/trial.js`、`services/billingPolicy.js`
> 是 M1↔M2 的**接缝**（套餐门禁 + 免费国家逻辑）。改套餐档位看 `planMatrix.js` 顶部「★ 套餐切口」。

---

## BUILDERHUB 业务层（marketplace，进行中）

| 模块 | 后端 | 前端 | Prisma 模型 |
|------|------|------|------------|
| **M10 任务市场** | `routes/projects.js`（发布/编辑/审核提交/取消/市场浏览/交付/验收） `routes/adminProjects.js` `constants/marketplace.js`（状态机+费率单一出处） | `pages/projects/{Marketplace,ProjectDetail,PostProject,MyProjects}.tsx` `pages/admin/AdminProjects.tsx` `api/projects.ts` | Project |
| **M11 申请接单** | `routes/applications.js`（投标/撤回/录用/拒绝，录用时建托管单） `routes/builders.js`（Builder 资料 upsert + 作品集 CRUD + 公开主页，投标前置） | `pages/projects/MyApplications.tsx` `pages/{BuilderProfile,BuilderPublicProfile}.tsx` `api/{applications,builders}.ts` + ProjectDetail 内嵌申请/交付/验收 UI | Application, BuilderProfile, PortfolioItem |
| **M12 托管支付** | `services/escrow.js`（资金状态机+账本，MOCK provider） `routes/escrow.js`（付款/纠纷/钱包） `routes/adminDisputes.js`（仲裁：放款/退款/部分退款） `workers/autoConfirm.js`（交付超时自动放款，RUN_BG_WORKERS） | `components/EscrowPanel.tsx` `pages/Wallet.tsx` `pages/admin/AdminDisputes.tsx` `api/escrow.ts` | EscrowTransaction, Dispute, Wallet, WalletLedger |
| **M13 双向盲评** | `routes/reviews.js`（双方提交后同时发布；客户评分计入 Builder 信誉） | `components/ReviewSection.tsx` `api/reviews.ts` | Review |
| **M14 站内私信** | `routes/conversations.js`（会话粒度 = 任务×Builder；未读计数；联系方式交换 contactFlag 软标记） | `pages/Messages.tsx` `api/messages.ts` + AppLayout 未读角标轮询 | Conversation, Message |
| **M15 Builder 认证** | `routes/adminBuilders.js`（Verified 通过/撤销 + writeAdminLog） | `pages/admin/AdminBuilders.tsx` | （BuilderProfile.verified*） |
| **待建** | 真实支付渠道（Stripe Connect 换 MOCK provider）、提现、里程碑分段托管（V1.5）、消息实时推送（现为轮询） | — | — |

---

## BUSINESS 业务示例层（DELF，可整体替换/删除）

> **状态（2026-07-28）**：DELF 业务文件已从本仓库删除，下表仅作骨架来源记录；
> 品牌与 i18n 残留已清理（mailer 署名、语言文件废弃命名空间、旧 dist 构建产物）。

| 示例模块 | 后端 | 前端 | Prisma 模型 |
|---------|------|------|------------|
| **B1 题库/考试** | `routes/{exams,sessions,examAudio,userExamSets,adminExams}.js` `services/{examImport,grader,prediction,questionExplainer}.js` `utils/{examTitle,examSetYear,audioCdn,audioToken}.js` `constants/{delfRubric,delfScoring,delfOralRubric,essayTemplateDefaults}.js` `scripts/*`(题库导入) | `pages/{Dashboard,DashboardPrediction,PracticeHub,SkillPractice,ExamRunner,ReviewResult,MyExams,MyExamEdit,MistakeNotebook,ExamGuide}.tsx` `pages/admin/{AdminExams,AdminExamEdit,AdminExamImport}.tsx` `components/{AudioPlayer,CoSectionRunner,CoSectionRunnerMock,SkillRadarChart,ScorePredictionCard,ExamGuideContent,LazyECharts}.tsx` | ExamSet, AudioDocument, ReadingPassage, Question, QuestionOption, ExamSession, UserAttempt |
| **B2 AI 批改/口语** | `routes/{essays,essayTemplates,orals,recordings}.js` `services/{aiGrader,oralGrader,essayQueue,oralQueue,ocr,stt,reportPdf}.js` `utils/deepseekRequest.js` | `pages/SpeakingExam.tsx` `components/{EssayGradeCard,EssayInlineAnnotations,OralGradeCard,AiExplanation,AIModelPicker,AudioRecorder,TemplateDrawer,UpgradeDifferenceCard}.tsx` | Essay, EssayTemplate, OralFollowUp, Recording, Oral |

业务专属 npm 依赖（删业务可一并去掉）：`tesseract.js`(OCR) `pdf-parse` `pdfkit`(成绩单PDF) `openai`(DeepSeek/Whisper) `alipay-sdk`/`wechatpay-axios-plugin`(若不用国内直连)。

---

## 删除业务清单（把骨架变成纯平台）

1. **后端 schema**：删 `backend/prisma/schema/90-business-delf.prisma`；
   在 `backend/prisma/schema/00-core.prisma` 的 `User` 里删「业务示例关系」整段（已用 `↓↓↓/↑↑↑` 框好）。
2. **后端装配**：`backend/src/index.js` 删 3 处业务围栏段（require / BUSINESS 挂载 / business worker + drain），
   以及 ADMIN 段里 `adminExams` 那一行。
3. **后端文件**：删上表 B1/B2 列出的 `routes/services/utils/constants/scripts` 业务文件。
4. **前端**：`frontend/src/App.tsx` 删业务 lazy import 段 + 业务路由段 + 后台 exams 3 行；删上表 B1/B2 前端文件。
5. **依赖/资源**：按需从两个 `package.json` 移除业务依赖；删 `backend/content`、`backend/assets`、`*.traineddata`（本骨架未带）。
6. **迁移**：`npx prisma migrate dev --name drop_business`（基于新 schema 生成去掉业务表的迁移）。

> 反向操作（换新业务）：照着 `90-business-delf.prisma` 的写法新建你的领域 schema 文件，
> 在 `index.js` 业务围栏处挂你的路由，在 `App.tsx` 业务围栏处加你的页面即可。
